{{ 'section-main-page.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /* ENHANCED STICKY NAVIGATION STYLES */
  .sticky-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 9999;
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    transform: translateY(-100%);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 32px rgba(0, 0, 0, 0.08);
  }

  .sticky-nav.visible {
    transform: translateY(0);
  }

  .sticky-nav.scrolled {
    background: rgba(255, 255, 255, 0.96);
    box-shadow: 0 8px 40px rgba(0, 0, 0, 0.12);
  }

  .sticky-nav-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
  }

  .sticky-nav-logo {
    font-size: 1.5rem;
    font-weight: 700;
    color: #1a1a1a;
    text-decoration: none;
    transition: color 0.3s ease;
    z-index: 10;
  }

  .sticky-nav-logo:hover {
    color: #007bff;
  }

  .sticky-nav-menu {
    display: flex;
    gap: 2.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .sticky-nav-menu li {
    position: relative;
  }

  .sticky-nav-menu a {
    color: #333;
    text-decoration: none;
    font-weight: 500;
    font-size: 0.95rem;
    padding: 0.5rem 0;
    transition: all 0.3s ease;
    position: relative;
  }

  .sticky-nav-menu a::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 2px;
    background: #007bff;
    transition: width 0.3s ease;
  }

  .sticky-nav-menu a:hover {
    color: #007bff;
  }

  .sticky-nav-menu a:hover::after {
    width: 100%;
  }

  .sticky-nav-cta {
    background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
    color: white;
    padding: 0.75rem 1.75rem;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
    border: none;
    cursor: pointer;
  }

  .sticky-nav-cta:hover {
    background: linear-gradient(135deg, #0056b3 0%, #004085 100%);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 123, 255, 0.4);
  }

  /* Enhanced Mobile hamburger menu */
  .sticky-nav-mobile-toggle {
    display: none;
    background: none;
    border: none;
    font-size: 1.5rem;
    color: #333;
    cursor: pointer;
    padding: 0.5rem;
    border-radius: 4px;
    transition: all 0.3s ease;
    z-index: 10;
  }

  .sticky-nav-mobile-toggle:hover {
    background: rgba(0, 0, 0, 0.05);
  }

  .sticky-nav-mobile-toggle.active {
    color: #007bff;
  }

  .sticky-nav-menu.mobile-open {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(20px);
    flex-direction: column;
    padding: 2rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
    gap: 1.5rem;
    animation: slideDown 0.3s ease-out;
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .sticky-nav-menu.mobile-open a {
    font-size: 1.1rem;
    padding: 0.75rem 0;
    border-bottom: 1px solid rgba(0, 0, 0, 0.05);
  }

  .sticky-nav-menu.mobile-open a:last-child {
    border-bottom: none;
  }

  /* Scroll progress indicator */
  .scroll-progress {
    position: absolute;
    bottom: 0;
    left: 0;
    height: 3px;
    background: linear-gradient(90deg, #007bff, #00d4ff);
    transition: width 0.1s ease;
    border-radius: 0 2px 2px 0;
  }

  /* Mobile responsive improvements */
  @media screen and (max-width: 768px) {
    .sticky-nav-content {
      padding: 0.75rem 1rem;
    }
    
    .sticky-nav-menu {
      display: none;
    }
    
    .sticky-nav-menu.mobile-open {
      display: flex;
    }
    
    .sticky-nav-mobile-toggle {
      display: block;
    }
    
    .sticky-nav-logo {
      font-size: 1.25rem;
    }
    
    .sticky-nav-cta {
      padding: 0.6rem 1.2rem;
      font-size: 0.85rem;
    }
  }

  /* Dark mode support */
  @media (prefers-color-scheme: dark) {
    .sticky-nav {
      background: rgba(20, 20, 20, 0.92);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .sticky-nav.scrolled {
      background: rgba(20, 20, 20, 0.96);
    }

    .sticky-nav-logo,
    .sticky-nav-menu a,
    .sticky-nav-mobile-toggle {
      color: #fff;
    }

    .sticky-nav-menu.mobile-open {
      background: rgba(20, 20, 20, 0.98);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
  }

  /* TRUE Full-width container styles - No side margins */
  .main-page-full-width {
    width: 100vw !important;
    max-width: none !important;
    margin: 0 !important;
    padding: 0 !important;
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw !important;
    margin-right: -50vw !important;
  }

  .main-page-content {
    width: 100% !important;
    max-width: none !important;
    padding: 0 !important;
    margin: 0 !important;
  }

  .rte.full-width {
    width: 100% !important;
    padding: 0 !important;
    margin: 0 !important;
  }

  /* Override ALL Shopify container constraints */
  .main-page-full-width .page-width,
  .main-page-full-width .page-width--narrow,
  .main-page-full-width .container,
  .main-page-full-width .section-padding {
    width: 100% !important;
    max-width: none !important;
    padding-left: 0 !important;
    padding-right: 0 !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
  }

  /* Ensure content inside takes full width */
  .main-page-full-width * {
    max-width: none !important;
  }

  /* Navigation pills full width styling */
  .main-page-full-width .nav-pills {
    width: 100%;
    padding: 0 2rem;
    justify-content: center;
    gap: 2rem;
  }

  .main-page-full-width .nav-pill {
    padding: 1rem 2rem;
    min-width: 150px;
    font-size: 1rem;
  }

  /* Services grid full width */
  .main-page-full-width .services-grid {
    width: 100%;
    max-width: none !important;
    padding: 0 2rem;
    gap: 3.5rem;
  }

  /* Hero section full width */
  .main-page-full-width .hero {
    width: 100%;
    margin: 0;
    padding: 0;
  }

  .main-page-full-width .hero-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Timing and CTA sections full width */
  .main-page-full-width .timing-section,
  .main-page-full-width .cta-section {
    width: 100%;
    margin: 0;
    padding-left: 0;
    padding-right: 0;
  }

  @media screen and (max-width: 749px) {
    .main-page-full-width .nav-pills {
      padding: 0 1rem;
      gap: 1rem;
    }
    
    .main-page-full-width .services-grid {
      padding: 0 1rem;
    }
    
    .main-page-full-width .hero-content {
      padding: 0 1rem;
    }
  }

  .service-card {
    background: #fff;
    border-radius: 1rem;
    box-shadow: 0 2px 16px rgba(0,0,0,0.07);
    padding: 2rem 1.5rem 1.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    transition: all 0.3s ease;
  }

  .service-card:hover {
    box-shadow: 0 8px 40px rgba(0,0,0,0.15);
    transform: translateY(-4px);
  }

  .service-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: #222;
  }

  .service-card ul {
    padding-left: 1.2rem;
    margin: 0;
    color: #444;
    font-size: 1rem;
    line-height: 1.7;
  }

  .service-card li {
    margin-bottom: 0.7rem;
  }
{%- endstyle -%}

<!-- ENHANCED STICKY NAVIGATION HTML -->
<div class="sticky-nav" id="stickyNav">
  <div class="sticky-nav-content">
    <a href="/" class="sticky-nav-logo" aria-label="Home">{{ shop.name }}</a>
    <nav role="navigation" aria-label="Main navigation">
      <ul class="sticky-nav-menu" id="stickyNavMenu">
        <li><a href="#services" aria-label="View our services">Services</a></li>
        <li><a href="#about" aria-label="Learn about us">About</a></li>
        <li><a href="#portfolio" aria-label="View our portfolio">Portfolio</a></li>
        <li><a href="#contact" aria-label="Contact us">Contact</a></li>
      </ul>
    </nav>
    <button class="sticky-nav-mobile-toggle" id="mobileToggle" aria-label="Toggle mobile menu" aria-expanded="false">
      <span>☰</span>
    </button>
    <a href="#contact" class="sticky-nav-cta" aria-label="Get started with our services">Get Started</a>
  </div>
  <div class="scroll-progress" id="scrollProgress"></div>
</div>

<div class="main-page-full-width section-{{ section.id }}-padding">
  <div class="main-page-content">
    <!-- PAGE TITLE REMOVED - No more annoying header! -->
    <div class="rte full-width{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
      {{ page.content }}
    </div>
  </div>
</div>

<!-- MODERN SERVICES GRID (MATCHES EXISTING STYLE) -->
<div class="services-grid" id="services" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 2.5rem; margin-top: 2.5rem;">
  <!-- Consulting & Strategy -->
  <div class="service-card">
    <h3>Consulting & Strategy</h3>
    <ul>
      <li>Business & digital strategy sessions</li>
      <li>Brand positioning & market research</li>
      <li>Project management & launch planning</li>
    </ul>
  </div>

  <!-- Mobile & App Services -->
  <div class="service-card">
    <h3>Mobile & App Services</h3>
    <ul>
      <li>Mobile app design & development</li>
      <li>App store optimization</li>
      <li>Progressive web apps (PWAs)</li>
    </ul>
  </div>

  <!-- AI & Automation Services -->
  <div class="service-card">
    <h3>AI & Automation Services</h3>
    <ul>
      <li>AI-powered chatbots & automations</li>
      <li>Workflow automation & integrations</li>
      <li>Custom AI solutions for business</li>
    </ul>
  </div>

  <!-- Web Design & Development -->
  <div class="service-card">
    <h3>Web Design & Development</h3>
    <ul>
      <li><strong>LaunchSite Basic – $299 CAD</strong><br>5-page website (WordPress or Shopify), content writing, banners, 3-month hosting, SEO setup</li>
      <li><strong>BrandSite Essential – $399 CAD</strong><br>7-page custom website with design, content, SEO setup, banners, project manager</li>
      <li><strong>BrandSite Plus – $599 CAD</strong><br>More advanced pages & layout, extra customization</li>
      <li><strong>EliteSite Pro – $699 CAD</strong><br>Full-service premium website with concierge-level support & UX/UI optimization</li>
      <li><strong>Ultimate Growth Bundle – $1,097 CAD</strong><br>Premium Website + Premium Design Subscription + Analytics Setup</li>
    </ul>
  </div>

  <!-- Creative Subscriptions -->
  <div class="service-card">
    <h3>Creative Subscriptions (Monthly Design Plans)</h3>
    <ul>
      <li><strong>DesignLite Monthly – $299 CAD/mo</strong><br>Unlimited static/animated ads, social posts, logo & simple visuals</li>
      <li><strong>DesignPlus Monthly – $399 CAD/mo</strong><br>Adds landing pages, ebooks, merch, basic motion design</li>
      <li><strong>DesignPro Monthly – $499 CAD/mo</strong><br>Project manager, 15 hrs/mo, complex campaigns & heavy illustrations</li>
      <li><strong>Influencer Igniter – $1,675 CAD</strong><br>Full branding kit, templates, content & strategy for creators (one-time)</li>
    </ul>
  </div>

  <!-- Marketing & Analytics Services -->
  <div class="service-card">
    <h3>Marketing & Analytics Services</h3>
    <ul>
      <li><strong>InsightBoost Setup – $299 CAD</strong><br>Google Analytics 4 setup + event tracking + support</li>
      <li><strong>LocalRank Starter – $199 CAD</strong><br>Google My Business profile optimization</li>
      <li><strong>GrowthBlock Pro – $499 CAD</strong><br>Design + brand strategy for new companies</li>
      <li><strong>MicroTask Boost – from $127 CAD</strong><br>Small one-off marketing/design jobs</li>
    </ul>
  </div>

  <!-- Paid Ad Support Services -->
  <div class="service-card">
    <h3>Paid Ad Support Services</h3>
    <ul>
      <li><strong>AdLaunch Google – $299 CAD</strong><br>Google Ads setup or audit</li>
      <li><strong>AdLaunch Meta – $299 CAD</strong><br>Facebook/Instagram ad creative & setup</li>
    </ul>
  </div>

  <!-- Hosting -->
  <div class="service-card">
    <h3>Hosting</h3>
    <ul>
      <li><strong>CoreHost 1-Year – $169 CAD</strong><br>1-year hosting + email + support</li>
      <li><strong>CoreHost 3-Year – $244.90 CAD</strong><br>3-year hosting plan</li>
    </ul>
  </div>

  <!-- Brand Identity Design -->
  <div class="service-card">
    <h3>Brand Identity Design</h3>
    <ul>
      <li><strong>LogoStart – $208 CAD</strong><br>Simple business logo (2 concepts)</li>
      <li><strong>LogoPro Kit – $283 CAD</strong><br>High-res business logo with brand colors & fonts</li>
      <li><strong>CardCraft Design – $156 CAD</strong><br>Custom-designed business cards (print-ready)</li>
    </ul>
  </div>

  <!-- Printed Marketing Collateral -->
  <div class="service-card">
    <h3>Printed Marketing Collateral</h3>
    <ul>
      <li><strong>PrintCards (500) – $152 CAD</strong><br>Premium business cards</li>
      <li><strong>RackImpact (250) – $152 CAD</strong><br>Rack cards for local promotion</li>
      <li><strong>PostMax (250) – $152 CAD</strong><br>Postcards for direct mail campaigns</li>
    </ul>
  </div>

  <!-- Gift Cards -->
  <div class="service-card">
    <h3>Gift Cards</h3>
    <ul>
      <li><strong>BrandBuilder Gift Card – $125 CAD</strong><br>Buy $100, Get $125 credit promo</li>
    </ul>
  </div>
</div>
<!-- END MODERN SERVICES GRID -->

<!-- ENHANCED STICKY NAVIGATION JAVASCRIPT -->
<script>
document.addEventListener('DOMContentLoaded', function() {
  const stickyNav = document.getElementById('stickyNav');
  const mobileToggle = document.getElementById('mobileToggle');
  const navMenu = document.getElementById('stickyNavMenu');
  const scrollProgress = document.getElementById('scrollProgress');
  let lastScrollTop = 0;
  let scrollThreshold = 80;
  let isScrolling = false;
  
  // Enhanced scroll handling with scroll progress
  function handleScroll() {
    const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    const documentHeight = document.documentElement.scrollHeight - window.innerHeight;
    const scrollPercent = (scrollTop / documentHeight) * 100;
    
    // Update scroll progress bar
    scrollProgress.style.width = scrollPercent + '%';
    
    // Add scrolled class for enhanced styling
    if (scrollTop > 50) {
      stickyNav.classList.add('scrolled');
    } else {
      stickyNav.classList.remove('scrolled');
    }
    
    // Show/hide navigation based on scroll direction
    if (scrollTop > scrollThreshold) {
      if (scrollTop > lastScrollTop && scrollTop > 200) {
        // Scrolling down - hide nav
        stickyNav.classList.remove('visible');
      } else {
        // Scrolling up - show nav
        stickyNav.classList.add('visible');
      }
    } else {
      // At top of page - hide nav
      stickyNav.classList.remove('visible');
    }
    
    lastScrollTop = scrollTop;
    isScrolling = false;
  }
  
  // Optimized scroll event handling
  function requestTick() {
    if (!isScrolling) {
      requestAnimationFrame(handleScroll);
      isScrolling = true;
    }
  }
  
  window.addEventListener('scroll', requestTick, { passive: true });
  
  // Enhanced mobile menu toggle with accessibility
  mobileToggle.addEventListener('click', function(e) {
    e.stopPropagation();
    const isOpen = navMenu.classList.contains('mobile-open');
    
    navMenu.classList.toggle('mobile-open');
    mobileToggle.classList.toggle('active');
    mobileToggle.setAttribute('aria-expanded', !isOpen);
    
    // Update hamburger icon
    const icon = mobileToggle.querySelector('span');
    icon.textContent = isOpen ? '☰' : '✕';
  });
  
  // Close mobile menu when clicking outside
  document.addEventListener('click', function(e) {
    if (!stickyNav.contains(e.target) && navMenu.classList.contains('mobile-open')) {
      navMenu.classList.remove('mobile-open');
      mobileToggle.classList.remove('active');
      mobileToggle.setAttribute('aria-expanded', 'false');
      mobileToggle.querySelector('span').textContent = '☰';
    }
  });
  
  // Enhanced smooth scroll with offset for sticky nav
  function smoothScrollTo(target) {
    const targetElement = document.querySelector(target);
    if (targetElement) {
      const offsetTop = targetElement.offsetTop - 80; // Account for sticky nav height
      window.scrollTo({
        top: offsetTop,
        behavior: 'smooth'
      });
    }
  }
  
  // Smooth scroll for navigation links with active state
  document.querySelectorAll('.sticky-nav-menu a, .sticky-nav-cta').forEach(link => {
    link.addEventListener('click', function(e) {
      const href = this.getAttribute('href');
      if (href.startsWith('#')) {
        e.preventDefault();
        
        // Close mobile menu if open
        navMenu.classList.remove('mobile-open');
        mobileToggle.classList.remove('active');
        mobileToggle.setAttribute('aria-expanded', 'false');
        mobileToggle.querySelector('span').textContent = '☰';
        
        // Smooth scroll to target
        smoothScrollTo(href);
        
        // Update active link
        document.querySelectorAll('.sticky-nav-menu a').forEach(navLink => {
          navLink.classList.remove('active');
        });
        this.classList.add('active');
      }
    });
  });
  
  // Intersection Observer for active navigation states
  const sections = document.querySelectorAll('[id]');
  const navLinks = document.querySelectorAll('.sticky-nav-menu a[href^="#"]');
  
  const observerOptions = {
    rootMargin: '-100px 0px -50% 0px',
    threshold: 0
  };
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const activeLink = document.querySelector(`.sticky-nav-menu a[href="#${entry.target.id}"]`);
        if (activeLink) {
          navLinks.forEach(link => link.classList.remove('active'));
          activeLink.classList.add('active');
        }
      }
    });
  }, observerOptions);
  
  sections.forEach(section => {
    observer.observe(section);
  });
  
  // Keyboard navigation support
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape' && navMenu.classList.contains('mobile-open')) {
      navMenu.classList.remove('mobile-open');
      mobileToggle.classList.remove('active');
      mobileToggle.setAttribute('aria-expanded', 'false');
      mobileToggle.querySelector('span').textContent = '☰';
    }
  });
});
</script>

{% schema %}
{
  "name": "t:sections.main-page.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
}
{% endschema %}